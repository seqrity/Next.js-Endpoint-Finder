
![Demo](./demo.gif)


Bookmarklet:
```
javascript:(function(){  const manifest = window.__BUILD_MANIFEST ||                    (window.__NEXT_DATA__ && window.__NEXT_DATA__.props?.pageProps?.__N_SSG &&                     Object.keys(window.__NEXT_DATA__.props.pageProps).filter(k=>k.startsWith(%27/%27)));    if (!manifest || !manifest.sortedPages) {    alert(%27Warning: Manifest not found%27);    return;  }  const p = manifest.sortedPages;  const api = p.filter(r=>r.startsWith(%27/api%27));  const dynamic = p.filter(r=>r.includes(%27[%27) && !r.startsWith(%27/api%27) && !r.includes(%27[...%27));  const catchAll = p.filter(r=>r.includes(%27[...%27));  const stat = p.filter(r=>!r.includes(%27[%27) && !r.startsWith(%27/api%27));  const html = `<!DOCTYPE html><html><head><meta charset="UTF-8"><title>Next.js Routes</title><style>body{font-family:monospace;background:#fff;padding:20px}h1{color:#0070f3}h2{margin-top:20px;color:#0070f3}pre{background:#f5f5f5;padding:15px;border-left:4px solid #0070f3;overflow-x:auto}.btn{background:#0070f3;color:#fff;border:none;padding:8px 16px;margin:5px;cursor:pointer;border-radius:4px}</style></head><body>  <h1>Next.js Routes</h1>  <p><strong>Target:</strong> ${location.href}</p>  <button class="btn" onclick="copyAll()">Copy All</button>  <button class="btn" onclick="copyAPI()">Copy APIs</button>  <h2>API Routes (${api.length})</h2><pre>${api.join('\n')||'None'}</pre>  <h2>Dynamic Routes (${dynamic.length})</h2><pre>${dynamic.join('\n')||'None'}</pre>  <h2>Catch-All (${catchAll.length})</h2><pre>${catchAll.join('\n')||'None'}</pre>  <h2>Static Pages (${stat.length})</h2><pre>${stat.join('\n')||'None'}</pre>  <script>    function copyAll(){navigator.clipboard.writeText(${JSON.stringify(p)}.join('\\n')).then(()=>alert('Copied'))}    function copyAPI(){navigator.clipboard.writeText(${JSON.stringify(api)}.join('\\n')).then(()=>alert('Copied'))}  </script>  </body></html>%60;    const win = window.open();  win.document.write(html);})();
```
